---
description: Guardar y abrir una nueva sesion con handoff compacto
---
AOS nueva sesion.

Fallback de prompt: si estas leyendo este texto, la extension command `/aos-nueva-sesion` no intercepto el comando. Ejecuta `/reload` y vuelve a usar `/aos-nueva-sesion`, o usa `/aos-guardar-sesion` seguido de `/aos-continuar <objetivo>`.

La version extension crea una sesion Pi nueva con handoff desde docs vivos. No arranca con `aos-gol` salvo que uses `/aos-nueva-sesion-con-gol`.
