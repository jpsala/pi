---
description: Guardar, abrir nueva sesion y pedir que arranque con aos-gol
---
AOS nueva sesion con aos-gol.

Fallback de prompt: si estas leyendo este texto, la extension command `/aos-nueva-sesion-con-gol` no intercepto el comando. Ejecuta `/reload` y vuelve a usar `/aos-nueva-sesion-con-gol`, o usa `/aos-guardar-sesion` seguido de `/aos-continuar <objetivo>`.

La version extension crea una sesion Pi nueva con handoff desde docs vivos y arranca esa sesion con `aos-gol`.
