---
name: aos-init-os
description: Initialize a minimal downstream AOS installation in a new or empty project with honest local placeholders, without copying AOS manager-only context. Use when JP says `init os`, `armame un sistema agentico aca`, or starts a new repo.
---

# Init OS

Crear AOS minimo en un proyecto nuevo.

Fuente canonica: `docs/topics/agentic-os-operations.md`, seccion `Init`.

Leer primero el destino, detectar stack y docs existentes, crear estructura minima local, usar placeholders honestos, agregar adapters solo si aplican o se piden, no copiar registry/decisiones/tracks/memoria del kit canonico, regenerar indice y correr audit.
