---
name: init-os
description: Initialize AOS in a new or empty project with honest placeholders and minimal agentic structure. Use when JP says `init os`, `armame un sistema agentico aca`, or starts a new repo.
---

# Init OS

Crear AOS minimo en un proyecto nuevo.

Fuente canonica: `docs/topics/agentic-os-operations.md`, seccion `Init`.

Leer primero el destino, detectar stack y docs existentes, crear estructura minima, usar placeholders honestos, agregar adapters solo si aplican o se piden, regenerar indice y correr audit.
